include utils.pug

html
  head
    meta(name="description" content="GoodGames")
    meta(name="title" property="og:title" content="GoodGames")
    meta(property="og:type" content="Website")
    meta(name="image" property="og:image" content="https://live.staticflickr.com/65535/51142975341_78af3c55e3_z.jpg")
    meta(name="description" property="og:description" content="GoodGames is an game cataloging website that allows individuals to rate, comment on, and browse video games of all types. Users can sign up and register to add games to their Game Center to let others know what games they want to play, what they are currently playing, and what they have already played.")
    meta(name="author" content="GoodGames")
    title="GoodGames"
    link(rel='stylesheet' href='/stylesheets/log-in.css')
    link(rel="preconnect" href="https://fonts.gstatic.com")
    link(href="https://fonts.googleapis.com/css2?family=Ubuntu&display=swap" rel="stylesheet")
  body.login_body.background_image
      .login_content
        +validationErrorSummary(errors)
        #title
          img(src="/images/title_header.png", alt="", srcset="")
        form(action='/login' method='post')
            input(type='hidden' name='_csrf' method='post' value=csrfToken)
            div(class="main")
                p(class="sign" align="center") Login
                    div(class="sign")
                        input(type='text' name='username' class='un' placeholder='Enter Username' required)
                        input(type='email' name='email' class='un' placeholder='Email' required)
                        input(type='password' name='password' class='un' placeholder='Password' required)
                        button(type='submit' class='submit') Login
                        div
                          a(href='/signup' class="missing") Don't Have an Account? Click Here.
                          a(href='/login/demo' class="missing") Or click here to take the site for a test drive!
